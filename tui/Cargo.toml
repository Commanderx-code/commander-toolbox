[package]
name = "linutil_tui"
description = "Commander Toolbox - a personalized Linux toolbox based on Linutil."
documentation = "https://christitustech.github.io/linutil"
readme = "../README.md"
edition = "2021"
license.workspace = true
repository = "https://github.com/ChrisTitusTech/linutil/tree/main/tui"
version.workspace = true
include = [
    "src/*.rs",
    "Cargo.toml",
    "cool_tips.txt",
    "assets/ctt_logo.png",
    "../man/commander-toolbox.1",
]

[[bin]]
name = "commander-toolbox"
path = "src/main.rs"

[features]
default = ["tips"]
tips = ["rand"]

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
image = { version = "0.25.10", default-features = false, features = ["ico", "png"] }
linutil_core = { version = "26.7.17", path = "../core" }
nix = { version = "0.31.3", features = ["user"] }
oneshot = { version = "0.2.1", features = ["std"], default-features = false }
portable-pty = "0.9.0"
rand = { version = "0.10.1", optional = true }
ratatui = { version = "0.30.0", features = ["crossterm"], default-features = false }
ratatui-image = { version = "11.0.5", default-features = false, features = ["crossterm", "image-defaults"] }
time = { version = "0.3.47", features = ["formatting", "local-offset", "macros"], default-features = false }
tree-sitter-bash = "0.25.1"
tree-sitter-highlight = "0.27.0"
tui-term = { version = "0.3.4", default-features = false }
unicode-width = { version = "0.2.0", default-features = false }
vt100-ctt = "0.17.1"
