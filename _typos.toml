[files]
extend-exclude = ["tui/cool_tips.txt"]

[default]
extend-ignore-identifiers-re = [
    "(?i)ratatui",
]

# Upstream GRUB theme name; keep its original spelling.
[default.extend-identifiers]
DedSec = "DedSec"
